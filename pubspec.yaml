name: positive_num
description: A value object that incapsulates a positive number that is greater than 0.
version: 1.0.1
repository: https://github.com/maxeema/positive_num

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
